#include <stdio.h>
#include <math.h>

int main()
{
    int  a, b, c;
    printf("Введите целые числа a, b и c: ");
    scanf("%d%d%d", &a, &b, &c);
    if ((sqrt(pow(a, 2) + pow(b, 2)) == c) || (sqrt(pow(b, 2) + pow(c, 2)) == a) || (sqrt(pow(c, 2) + pow(a, 2)) == b))
    {
        printf("Высказывание иcтиннo.");
    }
    else
    {
        printf("Высказывание лoжнo.");
    }
    return 0;
}
