#include <stdio.h>

int main()
{
    int  a, b;
    printf("Введите A и B: ");
    scanf("%d%d", &a, &b);
    if ((a > 2) & (b <= 3))
    {
        printf("Высказывание истинно.");
    }
    else
    {
        printf("Высказывание лoжнo.");
    }
    return 0;
}
